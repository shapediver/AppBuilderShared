import type {MantineCssStyleRecord} from "./primitives.schema-input";

/** Serializable subset of Mantine `Badge` props for theme `defaultProps`. */
export interface MantineBadgeProps {
	style?: MantineCssStyleRecord;
}
