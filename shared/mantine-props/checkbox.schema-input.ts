import type {MantineSpacing} from "./spacing.schema-input";

/** Serializable subset of Mantine `Checkbox` props for theme `defaultProps`. */
export interface MantineCheckboxProps {
	ml?: MantineSpacing;
	label?: string;
}
