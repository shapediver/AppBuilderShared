import type {MantineStylesApi} from "./primitives.schema-input";

/**
 * Serializable subset of Mantine `Accordion.Panel` props for theme `defaultProps`.
 * @strict
 */
export interface MantineAccordionPanelProps {
	styles?: MantineStylesApi;
}
