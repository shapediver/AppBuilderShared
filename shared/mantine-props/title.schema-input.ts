import type {MantineCssStyleRecord} from "./primitives.schema-input";

/** Serializable subset of Mantine `Title` props for theme `defaultProps`. */
export interface MantineTitleProps {
	style?: MantineCssStyleRecord;
}