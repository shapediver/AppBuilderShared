import type {MantineCssStyleRecord} from "./primitives.schema-input";

/** Serializable subset of Mantine `Menu.Dropdown` props for theme `defaultProps`. */
export interface MantineMenuDropdownProps {
	style?: MantineCssStyleRecord;
}