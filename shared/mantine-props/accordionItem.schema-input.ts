import type {MantineStylesApi} from "./primitives.schema-input";

/**
 * Serializable subset of Mantine `Accordion.Item` props for theme `defaultProps`.
 * @strict
 */
export interface MantineAccordionItemProps {
	styles?: MantineStylesApi;
}
