import type {MantineSizeToken} from "./spacing.schema-input";

/** Serializable subset of Mantine `TextInput` props for theme `defaultProps`. */
export interface MantineTextInputProps {
	size?: MantineSizeToken;
}
